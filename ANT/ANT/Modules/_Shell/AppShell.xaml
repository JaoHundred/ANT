<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:modules="clr-namespace:ANT.Modules"
             x:Class="ANT.Modules.AppShell"
             FlyoutHeaderBehavior="Fixed"
             x:DataType="modules:AppShell">

    <Shell.BindingContext>
        <modules:AppShellViewModel/>
    </Shell.BindingContext>

    <Shell.FlyoutHeader>
        <Grid>
            <Label Text="Teste flyout" FontSize="Header"/>
        </Grid>
    </Shell.FlyoutHeader>

    <FlyoutItem Title="Home" >
        <ShellContent ContentTemplate="{DataTemplate modules:HomeView}" />
    </FlyoutItem>
    <FlyoutItem Title="Settings" >
        <ShellContent ContentTemplate="{DataTemplate modules:SettingsView}" />
    </FlyoutItem>
    
    <ShellItem>
        <ShellSection>
            <ShellContent>
                <modules:HomeView/>
            </ShellContent>
        </ShellSection>
    </ShellItem>
</Shell>