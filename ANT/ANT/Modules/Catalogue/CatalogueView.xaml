<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:Event="clr-namespace:ANT._Behaviors"
             mc:Ignorable="d"
             
             x:Class="ANT.Modules.CatalogueView"
             >
    <!--<ContentPage.Behaviors>
        <Event:EventToCommandBehavior EventName="Appearing" Command="{Binding OnLoading}"/>
    </ContentPage.Behaviors>-->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Text="Carregar" Command="{Binding OnLoadingCommand}"/>
        <CollectionView ItemsSource="{Binding Animes}" Grid.Row="1" ItemSizingStrategy="MeasureAllItems" SelectionMode="Single" BackgroundColor="Transparent">
            <CollectionView.EmptyViewTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout VerticalOptions="Center" HorizontalOptions="Center">
                            <Label Text="Carregando..."/>
                            <ActivityIndicator IsRunning="True"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </CollectionView.EmptyViewTemplate>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Orientation="Horizontal" Padding="5">
                        <Image Source="{Binding ImageURL}" />
                        <Label Text="{Binding Title}" Grid.Column="1" VerticalOptions="Center" />
                    </StackLayout>
                    <!--ImageSource="{Binding ImageURL}"-->
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>


</ContentPage>