<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:Event="clr-namespace:ANT._Behaviors"
             xmlns:converter="clr-namespace:ANT.Converter"
             mc:Ignorable="d"
             
             x:Class="ANT.Modules.CatalogueView"
             BackgroundColor="{DynamicResource AppBackgroundColor}">

    <ContentPage.Resources>
        <converter:TextReductionConverter x:Key="ShortTextConverter"/>
    </ContentPage.Resources>
    <!--<ContentPage.Behaviors>
        <Event:EventToCommandBehavior EventName="Appearing" Command="{Binding OnLoading}"/>
    </ContentPage.Behaviors>-->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Text="Carregar" Command="{Binding OnLoadingCommand}"/>
        <CollectionView ItemsSource="{Binding Animes}" Grid.Row="1"  SelectionMode="Single"
                        >
            <CollectionView.ItemsLayout>
                <GridItemsLayout Orientation="Vertical" 
                                 Span="2" VerticalItemSpacing="10" HorizontalItemSpacing="10"/>
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame>
                        <Grid >
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>

                            <Image Source="{Binding ImageURL}" Style="{StaticResource AnimeFrameImage}" 
                                   />
                            <Label Grid.Row="1" Text="{Binding Title,Converter={StaticResource ShortTextConverter}}"
                                   Style="{StaticResource AnimeFrameLabel}" />
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>


</ContentPage>